name: Render Video Short

on:
  repository_dispatch:
    types: [create_short]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Sets up Python with fast pip caching
      - name: Set up Python with Caching
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      # Fast native install: ONLY ffmpeg & ffprobe (No bloat, takes ~3s)
      - name: Install FFmpeg and Dependencies
